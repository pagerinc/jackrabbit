version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
  - image: circleci/node:14.16@sha256:17fa9ef5127646cc148d9f6220cbd7b3b2c595f60bb9beb3ca4c71b4928f0180
  - image: rabbitmq:3.8-alpine@sha256:9c836867d693f53391ded42d9744fbc356e46d79b459582711d5a91454fe0890

jobs:
  test:
    <<: *defaults
    environment:
      RABBIT_URL=amqp://localhost
    steps:
    - checkout

    - run:
        name: Audit dependencies
        command: |
          set +e
          set +o pipefail
          if [ -f ".nsprc" ]; then
            sudo npm install --global --quiet audit-filter@0.5;
            npm audit --production --json | audit-filter;
          else
            npm audit --production;
          fi

    - restore_cache:
        keys:
        - v1-dependencies-{{ checksum "package.json" }}
        # fallback to using the latest cache if no exact match is found
        - v1-dependencies-

    - run: npm install
    - run: npm run lint

    - run:
        name: Wait for RabbitMQ to receive connections
        command: dockerize -wait tcp://localhost:5672 -timeout 1m
    - run:
        name: Run tests
        command: npm test

    - save_cache:
        paths:
        - node_modules
        key: v1-dependencies-{{ checksum "package.json" }}

    - persist_to_workspace:
        root: ~/repo
        paths: .

  deploy:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - run:
          name: Authenticate with registry
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/repo/.npmrc
      - run:
          name: Publish package
          command: npm publish

workflows:
  version: 2
  test-deploy:
    jobs:
      - test:
          filters:
            tags:
              only: /^v.*/
      - deploy:
          requires:
            - test
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/
